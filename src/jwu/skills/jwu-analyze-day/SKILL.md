---
name: jwu-analyze-day
description: Use when the user asks for a daily work analysis / summary of their Jira tasks and Bitbucket PRs (e.g. «дневной анализ», «что у меня по задачам сегодня», «разбери мой день», «day analyze», «/jwu-analyze-day»). Runs `jwu action day-analyze` to get full synced context and writes a concise plan straight into the chat.
---

# jwu: разбор рабочего дня

> **Воркспейс = где лежит код.** jwu работает в контексте *воркспейса*: он определяется
> автоматически по текущей папке, проверить — `jwu_workspace_current()`
> (bash: `jwu workspace current --json`).
>
> **Код проекта лежит ИМЕННО в папках воркспейса.** Их список с тегами приходит сразу
> в `jwu_workspace_current()` и в ответе `jwu_job_start()` (поля `paths` и `known_tags`),
> отдельный вызов нужен только чтобы отфильтровать по тегу — `jwu_workspace_paths(tag=…)`
> (bash: `jwu workspace paths`). Ищи и правь только в этих папках;
> остальное на диске к задаче отношения не имеет. У папок есть **теги** («legacy-бэкенд»,
> «новая-версия», «фронт») — по ним понятно, что где: `jwu_workspace_paths(tag="legacy-бэкенд")`.
> Не угадывай по имени каталога — спроси теги. Пользователь просит пометить папку —
> `jwu_workspace_tag(path, add=[...])` (bash: `jwu workspace tag <DIR> --add <тег>`).
>
> **У воркспейса есть ПРАВИЛА** — то, что знает о проекте человек, но не знает код:
> запреты (⛔ `constraint`), инструкции (`howto` — как поднять стенд, прогнать тесты),
> соглашения (`convention` — ветки, формат коммита), грабли (`gotcha`), справка (`info`).
> Правила приходят готовым текстом в поле `rules_md` — оно есть и в `jwu_workspace_current()`,
> и в ответе `jwu_job_start()`. Общие правила там целиком, у правил конкретных
> репозиториев — только заголовки. Как только понял, в какой папке правишь, возьми её
> правила целиком: `jwu_rules(tag="legacy-бэкенд")` (bash: `jwu rules --tag legacy-бэкенд`).
> **Записи `⛔ ЗАПРЕТ` обязательны к исполнению** и важнее твоего удобства: если правило
> мешает выполнить просьбу — не обходи его молча, скажи об этом пользователю.
> Пользователь формулирует правило уровня проекта («не пушь в develop», «стенд поднимается
> так-то») — предложи сохранить: `jwu_rule_add(title, text, kind, tag)`
> (bash: `jwu rule add "…" --kind constraint`).
>
> Если `jira_enabled=false` — Jira-команды и инструменты **не вызывай**: якорь работы там
> либо локальная фича (`jwu_features` / `jwu feature list`), либо ничего
> (`jwu job start --title "…"`). Если папка не привязана ни к одному воркспейсу — скажи
> об этом пользователю и предложи подключить проект.
> Если папка не привязана ни к одному воркспейсу (`source` не `cwd`) — **не логируй работу**:
> записи уедут в чужой контур. Сначала подключи проект — скилл **jwu-workspace-setup**
> (`jwu init`).

## Overview

Краткий разбор рабочего дня по данным из `jwu` (Jira + Bitbucket). Утилита делает фулл-синк и
отдаёт расширенный контекст + промпт; ты составляешь **сжатую сводку-план** и печатаешь её в чат.
НЕ углубляйся в детали — пользователь хочет обзор «что случилось и что делать», подробности он разберёт сам.

## Шаги

0. `jwu action day-analyze` **требует Jira**. Если в воркспейсе её нет — не зови команду, а собери
   сводку из локальных данных: `jwu features --json` (что в работе) + `jwu jobs --json` (что делалось).

1. Выполни команду и прочитай её stdout целиком (это контекст + инструкция):

   ```bash
   jwu action day-analyze
   ```

   Команда сама делает фулл-синк (может занять ~30-90с). Если она падает с ошибкой авторизации —
   сообщи пользователю и остановись (нужны токены/доступ, см. `jwu auth check`).

2. Проанализируй данные, **сочетая два среза**:
   - **текущие дельты** (раздел «Изменения с прошлого синка») — что произошло нового;
   - **текущее состояние** каждого PR и задачи — даже без свежей дельты состояние может
     требовать действия (например, PR давно апрувнут, но задача всё ещё не на тестах).

   Поля контекста, на которые опираются эвристики: в строке PR — `состояние:`
   (`конфликт` / `есть NEEDS_WORK` / `нет ревьюверов` / `ждёт апрувов` / `апрувы собраны`),
   `ревью:` (`имя:A` = апрув, `:NEEDS_WORK`, `:N`/прочее), `комментов:`, `обновлён:`
   (как давно); у задач — `[статус]`, `assignee:`; в «Упоминаниях» — дата, автор и
   пометка `· новое` (упоминание ещё не прочитано). Шапка содержит моё имя — по нему
   отличай «на мне» от «не на мне».

   Для каждого «моего» PR и задачи выведи **конкретный следующий шаг**, опираясь на
   состояние, а не только на дельту. Применяй эвристики (состояние → что написать):

   PR:
   - **Конфликт** (`состояние: конфликт`, или дельта `new_conflict`) → «поправить
     merge-конфликт». Если при этом апрувы собраны — это приоритет (иначе апрувы впустую).
   - **`апрувы собраны`, нет `NEEDS_WORK`, нет конфликта, НО задача не в статусе тестирования**
     → «PR готов — перевести задачу на тесты» (сменить статус). Статус задачи бери из «Мои
     задачи» по ключу из заголовка PR; «тестовые» статусы — содержащие TEST/ТЕСТ или вида
     «READY FOR TESTING» (названия зависят от проекта).
   - **`апрувы собраны`, конфликта/`NEEDS_WORK` нет, задача УЖЕ на тестах** → «готово,
     действий не нужно» (отметь явно, чтобы не висело).
   - **`есть NEEDS_WORK` / новые замечания** (дельта `new_pr_comment`) →
     «ответить/поправить по замечаниям»; коротко суть из комментов PR.
   - **Смешанное ревью** (есть и `:A`, и `:NEEDS_WORK`) → «закрыть замечания, не мержить».
   - **`нет ревьюверов`** → «назначить ревьюверов / попросить ревью».
   - **`ждёт апрувов` и много комментов, но апрувов нет** → ревью буксует, пнуть/уточнить.
   - **Новые коммиты после апрува** (дельта `new_pr_commit` при уже собранных апрувах) →
     «нужен переревью».
   - **Stale-PR** (`обновлён:` давно — недели) → «разморозить или закрыть».

   Ревью (раздел «PR на ревью» — там ты ревьювер):
   - **Ждут моего ревью** (нет ни одного апрува/ревью) → глянуть, это на мне; приоритет,
     если висит давно (`обновлён:`).

   Задачи и упоминания:
   - **Упоминание** (меня тегнули с просьбой/ожиданием) → уточни, ЧТО именно от меня
     хотят (текст упоминания идёт прямо в контексте), предложи ответ/действие.
     Пометка `· новое` (ещё не прочитано) усиливает приоритет. Упоминание — это событие,
     а не задача: не смешивай с «моими» задачами и не считай его своей работой.
   - **Задача на тестах и на мне** (`assignee:` = я) висит давно → «пнуть тестирование /
     проверить статус».
   - **`status_change`, сделанный не мной** → проверь, не требует ли это теперь действия от
     меня (например, вернули из тестов → доработать).
   - **Задача `resolved`/закрыта** (дельта `resolved`) → «закрыть работу, убрать из активных».
   - **Приоритетная задача** (High/Highest) без движения → вынеси наверх плана.

   База — события из дельт: новые комменты (`new_comment`/`new_pr_comment`), смена статуса
   (`status_change`), новые апрувы (`reviewer_approved`), новые PR (`new_pr`).

   Затем собери **краткий план** маркерами, сгруппировав по действиям (пустые разделы опускай):
   - **Срочно поправить** — конфликты, `NEEDS_WORK`.
   - **Двигать дальше** — апрувнутые PR → перевести задачу на тесты; задачи, ждущие смены статуса.
   - **Готово / можно мержить** — PR без блокеров (кратко, чтобы закрыть в голове).
   - **Ответить / упоминания** — что от меня просят и где (в первую очередь `· новое`).
   - **Глянуть на ревью** — PR, ждущие моего ревью.
   - **Прочие изменения** — остальное из дельт.

   Пиши сжато, без воды и без копирования всего контекста. Где уместно — указывай ключ
   задачи / `repo#id` PR. Не выдумывай: если для эвристики не хватает данных (например,
   не виден статус задачи по PR) — не утверждай, а отметь как «проверить».

3. Покажи план пользователю прямо в чате. Если по ходу дня из плана выросла конкретная работа —
   заводи её через **jwu-start-job** (`jwu job start`), там ей и место: план сам по себе нигде
   не хранится.

## Заметки

- Это read-only по отношению к Jira/Bitbucket и к локальной БД `jwu`: только чтение.
- Что делалось раньше — смотри в работах: `jwu jobs` / скилл **jwu-post-analyze-day**.
- Если данных мало (пустые разделы) — так и напиши коротко, не выдумывай.
